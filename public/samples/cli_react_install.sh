# Clone the ReactJsonUI repository
git clone https://github.com/aspect-apps/ReactJsonUI.git

# Navigate to the tools directory and add to PATH
cd ReactJsonUI/rjui_tools
export PATH="$PATH:$(pwd)/bin"

# Or use directly from the project
./rjui_tools/bin/rjui --help
