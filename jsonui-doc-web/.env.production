# Canonical origin for production builds (metadataBase, sitemap.xml, robots.txt).
# The site is hosted at jsonui.tanosys.com; without this, code falls back to
# the jsonui.dev default in src/app/{layout,sitemap,robots}.ts(x).
NEXT_PUBLIC_SITE_URL=https://jsonui.tanosys.com
