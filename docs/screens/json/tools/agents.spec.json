{
  "type": "screen_spec",
  "version": "1.0",
  "metadata": {
    "name": "Agents",
    "displayName": "Agents for Claude / Codex",
    "description": "Tools > Agents overview. Explains the 9-agent + 11-skill + 5-rule pack that teaches Claude Code (or Codex) how to run the spec-first JsonUI workflow. Four H2 sections (What ships / The conductor flow / Per-agent summary / Adding your own). ~10-min read. Secondary audience is the AI-first team (plan 00 priority 2) — the page should read like a control-plane reference, not an essay.",
    "platforms": [
      "web"
    ],
    "layoutFile": "tools/agents",
    "createdAt": "2026-04-23",
    "updatedAt": "2026-04-23"
  },
  "structure": {
    "components": [],
    "layout": {},
    "collection": null,
    "tabView": null
  },
  "stateManagement": {
    "states": [],
    "uiVariables": [
      {
        "name": "agents",
        "type": "[AgentRow]",
        "initial": "[]",
        "description": "Nine agent entries (conductor / define / ground / implement / test / debug / navigation-ios / navigation-android / navigation-web) with name + role + when-to-use."
      },
      {
        "name": "nextReadLinks",
        "type": "[NextReadLink]",
        "initial": "[]",
        "description": "Two closing cards: CLI tools + MCP server overview."
      }
    ],
    "eventHandlers": [
      {
        "name": "onAppear",
        "description": "Seed agents + nextReadLinks from the static v1 catalog."
      },
      {
        "name": "onNavigate",
        "params": [
          {
            "name": "url",
            "type": "String"
          }
        ],
        "description": "Client-side navigation."
      }
    ],
    "displayLogic": []
  },
  "dataFlow": {
    "diagram": "flowchart TD\n    VIEW[ToolsAgentsView] --> VM[ToolsAgentsViewModel]\n    VM -- agents --> VIEW\n    VM -- nextReadLinks --> VIEW\n    VIEW -- onNavigate(url) --> VM",
    "repositories": [],
    "useCases": [],
    "apiEndpoints": [],
    "viewModel": {
      "methods": [
        {
          "name": "onAppear",
          "description": "Seed both `agents` and `nextReadLinks` from module-scope static catalogs. Every AgentRow (9 entries: conductor / define / ground / implement / test / debug / navigation-ios / navigation-android / navigation-web) has its nameKey / roleKey / whenToUseKey pre-resolved through StringManager.getString with the tools_agents_ prefix. Two NextReadLink entries point at /tools/cli and /tools/mcp as the natural next reads."
        },
        {
          "name": "onNavigate",
          "params": [{ "name": "url", "type": "String" }],
          "description": "router.push(url). Hit by NextReadLink card taps and any inline CTA. Agents are descriptive rows — no per-row navigation in v1."
        }
      ],
      "vars": [
        {
          "name": "agents",
          "type": "Array(AgentRow)",
          "observable": true,
          "description": "9-row agent catalog, seeded once per mount."
        },
        {
          "name": "nextReadLinks",
          "type": "Array(NextReadLink)",
          "observable": true,
          "description": "2 closing cards, seeded once per mount."
        }
      ]
    },
    "customTypes": [
      {
        "name": "AgentRow",
        "properties": [
          {
            "name": "id",
            "type": "String"
          },
          {
            "name": "nameKey",
            "type": "String"
          },
          {
            "name": "roleKey",
            "type": "String"
          },
          {
            "name": "whenToUseKey",
            "type": "String"
          }
        ]
      },
      {
        "name": "NextReadLink",
        "properties": [
          {
            "name": "id",
            "type": "String"
          },
          {
            "name": "titleKey",
            "type": "String"
          },
          {
            "name": "descriptionKey",
            "type": "String"
          },
          {
            "name": "url",
            "type": "String"
          }
        ]
      }
    ]
  },
  "userActions": [
    {
      "action": "Tap a TOC entry",
      "processing": "TOC-internal scroll."
    },
    {
      "action": "Tap a NextReadLink card",
      "processing": "onNavigate(url)."
    }
  ],
  "validation": {
    "clientSide": [],
    "serverSide": []
  },
  "transitions": [
    {
      "trigger": "onNavigate(url)",
      "condition": "url is a spec-mapped tools URL or /",
      "destination": "Target spec screen or tab",
      "description": "v1 destinations: /, /tools/cli, /tools/mcp, /learn/installation."
    }
  ],
  "relatedFiles": [
    {
      "type": "Layout",
      "path": "docs/screens/layouts/tools/agents.json",
      "description": "Agents overview layout."
    },
    {
      "type": "Layout",
      "path": "docs/screens/layouts/cells/agent_row.json",
      "description": "One-line agent row with name / role / when-to-use."
    },
    {
      "type": "ViewModel",
      "path": "jsonui-doc-web/src/viewmodels/tools/ToolsAgentsViewModel.ts",
      "description": "Seeds the nine-agent catalog + nextReadLinks."
    },
    {
      "type": "View",
      "path": "jsonui-doc-web/src/app/tools/agents/page.tsx",
      "description": "Next.js route."
    }
  ],
  "notes": [
    "First live entry under the Tools tab. Flipping TOOLS_ENTRIES row 4 (agents) from 'upcoming' to 'live' in HomeViewModel finishes the task.",
    "The agents Collection uses a dedicated cells/agent_row layout rather than the generic learn_article_card — the row has three strings (name / role / whenToUse) and a distinct density, so pulling it into its own cell keeps the styling honest.",
    "Plan 11 (content-plan-agents.md) lists nine agents. This page is the INTRO — each agent deserves a dedicated page later, but v1 ships only the overview catalog.",
    "Priority-2 audience (plan 00) is teams running JsonUI through Claude Code / Codex. Copy stays practical: what to type, when to invoke which agent, how conductor dispatches. No brand prose."
  ]
}
