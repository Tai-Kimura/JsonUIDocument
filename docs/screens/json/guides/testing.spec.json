{
  "type": "screen_spec",
  "version": "1.0",
  "metadata": {
    "name": "Testing",
    "displayName": "Writing screen tests",
    "description": "Guides > Writing screen tests. Seven sections + TOC + next-reads. Corrects two factual errors from the earlier draft: there is no `jui test` command (the CLI is `jsonui-test` from the separate jsonui-test-runner package), and the runner invocation is not `jsonui-test run --platform web --base …`. Walks where tests live, screen vs flow test anatomy with real JSON, the full action/assert DSL from the runtime schemas, the three drivers (XCUITest / Espresso / Playwright), the jsonui-test CLI sub-commands, and CI wiring (no shipped harness).",
    "platforms": [
      "web"
    ],
    "layoutFile": "guides/testing",
    "createdAt": "2026-04-23",
    "updatedAt": "2026-04-24"
  },
  "structure": {
    "components": [],
    "layout": {},
    "collection": null,
    "tabView": null
  },
  "stateManagement": {
    "states": [],
    "uiVariables": [
      {
        "name": "nextReadLinks",
        "type": "[NextReadLink]",
        "initial": "[]",
        "description": "Two closing cards."
      }
    ],
    "eventHandlers": [
      {
        "name": "onAppear",
        "description": "Seed nextReadLinks."
      },
      {
        "name": "onNavigate",
        "params": [
          {
            "name": "url",
            "type": "String"
          }
        ],
        "description": "Client-side navigation."
      }
    ],
    "displayLogic": []
  },
  "dataFlow": {
    "diagram": "flowchart TD\n    VIEW --> VM",
    "viewModel": {
      "methods": [
        {
          "name": "onAppear",
          "description": "Seed nextReadLinks from the module-scope NEXT_READ_ENTRIES catalog (two rows: next_test_runner -> /tools/test-runner, next_navigation -> /guides/navigation) and stamp currentLanguage from StringManager.language. Each row's titleKey / descriptionKey is resolved through StringManager with the guides_testing_ namespace prefix."
        },
        {
          "name": "onNavigate",
          "params": [
            {
              "name": "url",
              "type": "String"
            }
          ],
          "description": "Client-side navigation via router.push(url). Destinations are the spec-mapped URLs enumerated in transitions: /, /tools/test-runner, /guides/navigation."
        }
      ],
      "vars": [
        {
          "name": "nextReadLinks",
          "type": "Array(NextReadLink)",
          "observable": true,
          "description": "Two closing 'read next' cards pointing at /tools/test-runner and /guides/navigation. Seeded by onAppear from the NEXT_READ_ENTRIES static catalog and re-seeded by onToggleLanguage."
        }
      ]
    },
    "repositories": [],
    "useCases": [],
    "apiEndpoints": [],
    "customTypes": [
      {
        "name": "NextReadLink",
        "properties": [
          {
            "name": "id",
            "type": "String"
          },
          {
            "name": "titleKey",
            "type": "String"
          },
          {
            "name": "descriptionKey",
            "type": "String"
          },
          {
            "name": "url",
            "type": "String"
          }
        ]
      }
    ]
  },
  "userActions": [
    {
      "action": "Tap a TOC entry",
      "processing": "TOC-internal scroll."
    },
    {
      "action": "Tap a NextReadLink card",
      "processing": "onNavigate(url)."
    }
  ],
  "validation": {
    "clientSide": [],
    "serverSide": []
  },
  "transitions": [
    {
      "trigger": "onNavigate(url)",
      "condition": "url is spec-mapped",
      "destination": "Target screen or tab",
      "description": "v1 destinations: /, /tools/test-runner, /guides/navigation."
    }
  ],
  "relatedFiles": [
    {
      "type": "Layout",
      "path": "docs/screens/layouts/guides/testing.json",
      "description": "Layout."
    },
    {
      "type": "ViewModel",
      "path": "jsonui-doc-web/src/viewmodels/guides/TestingViewModel.ts",
      "description": "VM."
    },
    {
      "type": "View",
      "path": "jsonui-doc-web/src/app/guides/testing/page.tsx",
      "description": "Page."
    }
  ],
  "notes": [
    "Live Guides entry. Flip GUIDES_ENTRIES row for 'testing' to 'live' in HomeViewModel when shipping.",
    "Seven sections: (1) where tests live (tree excerpt), (2) screen test anatomy with real JSON, (3) flow test anatomy with real JSON (sources + checkpoints + screen discriminator), (4) action/assert DSL as 2 bullets sourced from schemas/actions.schema.json, (5) drivers bullets (XCUITest / Espresso / Playwright + native test runners), (6) jsonui-test CLI (install / validate / generate sub-commands), (7) CI with GitHub Actions YAML excerpt.",
    "Corrects prior false claims: no `jui test` CLI exists, CLI is `jsonui-test` from jsonui-test-runner; no `jsonui-test run --platform web --base http://localhost:3000` syntax — the real CLI has `validate` and `generate test|description|doc|html` sub-commands.",
    "Deprecated strings: section_runner_* / toc_row_runner remain in strings.json as unreferenced dead entries to minimise cross-session merge risk."
  ]
}
