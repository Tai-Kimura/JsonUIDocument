{
  "type": "screen_spec",
  "version": "1.0",
  "metadata": {
    "name": "SpecParentSubSpec",
    "displayName": "Parent + sub specs",
    "description": "Pattern 2 of spec splitting — deep dive into screen_parent_spec + screen_sub_spec. When to use, how `subSpecs[]` and `metadata.parentSpec` reference each other, what validator treats as inherited vs child-owned, and what `jui build` does at merge time.",
    "platforms": ["web"],
    "layoutFile": "spec/parent-sub-spec",
    "createdAt": "2026-04-24",
    "updatedAt": "2026-04-24"
  },
  "structure": {
    "components": [],
    "layout": {},
    "collection": null,
    "tabView": null
  },
  "stateManagement": {
    "states": [],
    "uiVariables": [
      { "name": "nextReadLinks", "type": "[NextReadLink]", "initial": "[]", "description": "Follow-up cards." }
    ],
    "eventHandlers": [
      { "name": "onAppear",   "description": "Seed nextReadLinks." },
      { "name": "onNavigate", "params": [{ "name": "url", "type": "String" }], "description": "Client-side navigation." }
    ],
    "displayLogic": []
  },
  "dataFlow": {
    "diagram": "flowchart TD\n    VIEW[SpecParentSubSpecView] --> VM[SpecParentSubSpecViewModel]\n    VM -- nextReadLinks --> VIEW",
    "viewModel": {
      "methods": [
        { "name": "onAppear",   "description": "Seed nextReadLinks from NEXT_READ_ENTRIES." },
        { "name": "onNavigate", "params": [{ "name": "url", "type": "String" }], "description": "router.push." }
      ],
      "vars": [
        { "name": "nextReadLinks", "type": "Array(NextReadLink)", "observable": true, "description": "Closing cards." }
      ]
    },
    "repositories": [], "useCases": [], "apiEndpoints": [],
    "customTypes": [
      {
        "name": "NextReadLink",
        "properties": [
          { "name": "id",             "type": "String" },
          { "name": "titleKey",       "type": "String" },
          { "name": "descriptionKey", "type": "String" },
          { "name": "url",            "type": "String" }
        ]
      }
    ]
  },
  "userActions": [
    { "action": "Tap a NextReadLink card", "processing": "onNavigate(url)." }
  ],
  "validation": { "clientSide": [], "serverSide": [] },
  "transitions": [
    {
      "trigger": "onNavigate(url)",
      "condition": "url is /spec/layout-file or /spec/component-spec",
      "destination": "Target spec screen",
      "description": "v1 destinations: /spec/layout-file, /spec/component-spec."
    }
  ],
  "relatedFiles": [
    { "type": "Layout",    "path": "docs/screens/layouts/spec/parent-sub-spec.json",           "description": "Detail layout for Pattern 2." },
    { "type": "ViewModel", "path": "jsonui-doc-web/src/viewmodels/spec/ParentSubSpecViewModel.ts", "description": "Essay VM." },
    { "type": "View",      "path": "jsonui-doc-web/src/app/spec/parent-sub-spec/page.tsx",      "description": "Next.js route." }
  ],
  "notes": [
    "Phase 2 article 2/5: the parent + sub split. Uses ~6 sections to walk through why + how + what validator enforces + what jui build merges.",
    "No live examples of this pattern inside JsonUIDocument or whisky-find-agent yet — the CodeBlocks are illustrative (a Home screen with Feed + Notifications sub regions)."
  ]
}
