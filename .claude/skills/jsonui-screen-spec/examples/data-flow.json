{
  "_comment": "Data flow - repositories, API endpoints, mermaid diagram",
  "dataFlow": {
    "diagram": "flowchart TD\n    VIEW[View] --> VM[ViewModel]\n    VM --> UC[UseCase]\n    UC --> REPO[Repository]\n    REPO --> API[\"/api/v1/users\"]",
    "repositories": [
      {
        "name": "UserRepository",
        "methods": ["fetchUser(id: String)", "updateUser(user: User)"]
      }
    ],
    "useCases": [
      {
        "name": "UserUseCase",
        "methods": ["getUser(id: String)", "validateAndUpdateUser(name: String, email: String)"]
      }
    ],
    "apiEndpoints": [
      {
        "path": "/api/v1/users/{id}",
        "method": "GET",
        "request": null,
        "response": {
          "id": "String",
          "name": "String",
          "email": "String"
        },
        "notes": "Fetch user by ID"
      }
    ],
    "notes": "Data flow notes (optional)"
  }
}
